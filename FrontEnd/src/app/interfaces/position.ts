export interface CardProps{
    x: number;
    y: number;
    width: number;
    height: number;
    editMode: boolean;
    storedWidth: number;
    storedHeight: number;
    storedX: number;
    storedY: number;
}